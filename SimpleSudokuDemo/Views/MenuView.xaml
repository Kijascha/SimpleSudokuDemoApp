<UserControl x:Class="SimpleSudokuDemo.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleSudokuDemo.Views" xmlns:viewmodels="clr-namespace:SimpleSudokuDemo.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MenuViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="28" Foreground="Black" Text="MenuView" Margin="0 0 0 20" />
            <Button Content="Create" FontSize="28" Foreground="Black" Margin="0 0 0 5" Command="{ Binding NavigateToCreateViewCommand }" />
            <Button Content="Play" FontSize="28" Foreground="Black" Command="{ Binding NavigateToPlayViewCommand }" />
        </StackPanel>
    </Grid>
</UserControl>
